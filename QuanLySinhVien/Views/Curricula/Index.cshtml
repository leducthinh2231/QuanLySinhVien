@model IEnumerable<QuanLySinhVien.Models.Curriculum>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CurrID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CurrID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CurrID })
        </td>
    </tr>
}

</table>
